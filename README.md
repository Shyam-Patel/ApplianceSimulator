# Web Appliance Simulator

An appliance simulator written using Javascript & Node.js framework with an implementation of OAuth2 authentication.

## Setup
1) Clone repository
2) Run 'npm install' to install all application dependencies
3) Run 'npm start' to launch the application on 'localhost:5000'
